import React from "react";
import { FlexContainer } from "../styles";

const InputField = () => {
  return (
    <FlexContainer>
      <input />
      <button>send</button>
    </FlexContainer>
  );
};

export default InputField;
